version: 1.0

input:
  - hello_string
  - json_data

tasks:
  hello_task:
    action: core.echo
    input:
      message: |
        Testing first ST2 workflow: "{{ ctx().hello_string }}"
        json payload message: "{{ ctx().json_data.message }}"

